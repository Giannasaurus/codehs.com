/** Given an array, return the length of that array. */

function length(array) {
    return array.length
}

/** passed test cases
  * test case 1:
    > length([1, 2, 3])
  * test case 2:
    > length([])
  * test case 3:
    > (hidden case)
  * test case 4:
    > (hidden case) */
