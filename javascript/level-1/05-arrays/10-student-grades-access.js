/** Write a function getStudentGrade that takes two parameters: grades (an array of numbers) and index (a number).
    The function should return the grade at the specified index in the array. */

function getStudentGrade(grades, index) {
    return grades[index];
}

/** passed test cases
  * getStudentGrade([85, 92, 78, 90], 2)
  * getStudentGrade([70, 88, 94], 0)
  * getStudentGrade([100, 99, 98, 97], 3) */
