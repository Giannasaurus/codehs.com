/** Write a function sumFirstTwo that takes one parameter: scores (an array containing at least two numbers).
    The function should return the sum of the first two elements in the array. */

function sumFirstTwo(scores) {
  return scores[0] + scores[1];
}

/** passed test cases
  * sumFirstTwo([10, 20, 30])
  * sumFirstTwo([5, 15])
  * sumFirstTwo([100, 200, 300, 400]) */
