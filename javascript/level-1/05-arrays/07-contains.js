/** Write the function contains that will return true if an array contains the element el.

  * For example:
    > contains(['glorp', 'glop', 'gloop'], 'gloop') => true */

function contains(array, el) {
    return array.includes(el)
}

/** passed test cases
  * test case 1:
    > contains([1, 2, 3], 2)
  * test case 2:
    > contains(['squirtle', 'charmander', 'bulbasaur'], 'pikachu')
  * test case 3:
    > contains(['t', 'e', 'a', 'm'], 'i')
  * test case 4:
    > (hidden)
  * test case 5:
    > (hidden) */
