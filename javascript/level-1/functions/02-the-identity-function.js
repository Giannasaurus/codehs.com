/** Write the function identity which takes an input x and returns that input. */

function identity(x){
    return x
}

/** passed test cases
  * test case 1:
    > identity(1)
  * test case 2:
    > identity(0)
  * test case 3:
    > identity("hello")
  * test case 4:
    > identity(true) */
