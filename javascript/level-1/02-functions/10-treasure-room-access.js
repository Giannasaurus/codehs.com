/** Write a function canEnterVault that takes a boolean hasKey and returns ‘Access granted’ if true, and ‘Access denied’ if false. */

function canEnterVault(hasKey) {
	return hasKey ? "Access granted" : "Access denied";
}

/** passed test cases
  * */
