/*
Rearrange the following lines of code to define a function that calculates the average of an array of numbers, returning the result as a single value.
*/

function computeAverage(numbers) {
    let sum = 0;
    for (let numbers of numbers) {
        sum += number;
    }
    return sum / numbers.length;
}