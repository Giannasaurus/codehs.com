/*
Use the provided code snippets to construct a function called ‘greet’. This function should take a single parameter ‘name’ and return a greeting message like ‘Hello, name!’.
*/

function greet(name) {
    return 'Hello, ' + name + '!';
}