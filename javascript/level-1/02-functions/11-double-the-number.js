/** Write a function called doubleNumber that takes one parameter: num (a number).
  The function should return the number multiplied by 2. */

function doubleNumber(num) {
    return num*2;
}

/** passed test cases
  * doubleNumber(5)
  * doubleNumber(10)
  * doubleNumber(0) */
