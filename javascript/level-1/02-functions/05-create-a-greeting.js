/** Write a function called createGreeting that takes one parameter: name (a string).
    The function should return a greeting in the format: “Hello, [name]!” */

function createGreeting(name) {
    return `Hello, ${name}!`;
}

/** passed test cases
  * createGreeting("Alice")
  * createGreeting("Bob")
  * createGreeting("Charlie") */
