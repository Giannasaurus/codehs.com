/** Write a function getFullName that takes two parameters: firstName (a string) and lastName (a string).
    The function should return a single string combining the first and last name with a space in between. */

function getFullName(firstName, lastName) {
    return firstName + " " + lastName;
}

/** passed test cases
  * getFullName("John", "Doe")
  * getFullName("Jane", "Smith")
  * getFullName("Alice", "Johnson") */
